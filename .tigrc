# unbind
bind generic g none
bind main G none

# bind
bind generic <Ctrl-g> !?git gc
bind generic g move-first-line
bind generic G move-last-line
bind main G move-last-line
bind main - :toggle commit-title-graph
